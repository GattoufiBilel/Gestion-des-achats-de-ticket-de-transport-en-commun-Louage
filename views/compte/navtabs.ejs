<ul class="nav nav-tabs justify-content-end mb-3" id="myTab" role="tablist">
  <li class="nav-item">
    <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true"><i class="fas fa-user"></i> Profile</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" id="profile-tab" data-toggle="tab" href="#profile" role="tab" aria-controls="profile" aria-selected="false"><i class="fas fa-lock"></i> Mot de passe</a>
  </li>
  <% if(locals.userInfo.role === locals.Role.client) { %>
  <li class="nav-item">
    <a class="nav-link" id="contact-tab" data-toggle="tab" href="#contact" role="tab" aria-controls="contact"
      aria-selected="false"><i class="fas fa-trash-alt"></i> Désactiver compte</a>
  </li>
  <% } %>
</ul>
<div class="tab-content" id="myTabContent">
  <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab"><%- include('profile') -%></div>
  <div class="tab-pane fade" id="profile" role="tabpanel" aria-labelledby="profile-tab"><%- include('password') -%></div>
  <% if(locals.userInfo.role === locals.Role.client) { %>
  <div class="tab-pane fade" id="contact" role="tabpanel" aria-labelledby="contact-tab"><%- include('desactiver') -%></div>
  <% } %>
</div>